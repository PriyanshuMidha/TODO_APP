{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default desktop capabilities for FocusDock.",
  "windows": ["main", "sticky"],
  "permissions": [
    "core:default",
    "core:window:allow-set-always-on-top"
  ]
}
